import React, { ReactElement, HTMLProps } from 'react';

export interface {{name}}Props extends HTMLProps {

}

const {{name}} = (props: {{name}}Props): ReactElement => {
return (
<>
</>
);
}

export default {{name}};
