import React from 'react';
import { describe, it, render } from '@testing-library/react';
import {{name}} from '../{{name}}';

describe('{{name}}', () =>
    it('renders', () => {
        render(<{{name}} />);
    })
);
