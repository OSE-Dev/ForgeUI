import React from 'react';
import { describe, it, render } from '@testing-library/react';
import Common from '../index';

describe('Common', () =>
    it('renders', () => {
        render(<Common />);
    })
);
